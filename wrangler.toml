name = "sanskar"
account_id = "98ff924c3596db390018b978ab57d532"
compatibility_date = "2024-12-01"

# Serves everything in ./public as static assets — same pattern as blog + tools sites.
# No worker.js needed; Cloudflare handles routing automatically.
#
# Deploy:
#   wrangler login        (auth stays local, never committed)
#   wrangler deploy
#
# Live at: https://sanskar.jainamshah.workers.dev
#
# Do NOT add account_id or api_token here.

[assets]
directory = "./public"
not_found_handling = "single-page-application"
